import React from "react";
import styled from "styled-components";

const Logo = styled.p`
  font-weight: 700;
  font-size: 1.5rem;
`;

const BrandLogo = () => {
  return <Logo>Fiber</Logo>;
};

export default BrandLogo;
